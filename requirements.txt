# Core - needed by every approach
numpy>=1.26
pandas>=2.0
scikit-learn>=1.4
scipy>=1.11
pyarrow>=15.0          # parquet cache for the slimmed dataset
joblib>=1.3

# Notebooks
jupyter>=1.0
ipykernel>=6.29
matplotlib>=3.8

# Approach C only (mixture-of-experts). Not needed for A or B.
# torch>=2.2
